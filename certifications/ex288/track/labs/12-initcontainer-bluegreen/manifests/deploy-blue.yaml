apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue
spec:
  replicas: 1
  selector:
    matchLabels:
      color: blue
  template:
    metadata:
      labels:
        app: switchable
        color: blue
    spec:
      volumes:
        - name: shared
          emptyDir: {}
      initContainers:
        - name: init
          image: registry.access.redhat.com/ubi9/ubi-minimal
          command: ["/bin/sh", "-c", "echo blue > /work/color"]
          volumeMounts:
            - name: shared
              mountPath: /work
      containers:
        - name: app
          image: registry.access.redhat.com/ubi9/nginx-122
          volumeMounts:
            - name: shared
              mountPath: /usr/share/nginx/html
