apiVersion: v1
kind: Service
metadata:
  name: {{ include "basic-node.fullname" . }}
spec:
  selector:
    app: {{ include "basic-node.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
